import { AdditionalVisitorDialog } from './additional-visitor-dialog';

describe('AdditionalVisitorDialog', () => {
  it('should create an instance', () => {
    expect(new AdditionalVisitorDialog()).toBeTruthy();
  });
});
